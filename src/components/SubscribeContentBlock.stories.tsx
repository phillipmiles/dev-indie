import React from 'react';
import SubscribeContentBlock from './SubscribeContentBlock';

export default {
  title: 'Components/SubscribeContentBlock',
  component: SubscribeContentBlock,
};

const Template = (args) => <SubscribeContentBlock {...args} />;

export const Normal = Template.bind({});
